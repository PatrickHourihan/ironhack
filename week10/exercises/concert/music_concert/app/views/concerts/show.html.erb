<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<title>Music Concert Network</title>
</head>
<body>
<div class="container-fluid">

  <div class="row">

 	<div class="col-sm-12">
	<h1>Artist: <%= @concert.artist %></h1>
	<h4>Venue: <%= @concert.venue %></h4>
	<h4>City: <%= @concert.city %></h4>
	<h4>Date: <%= @concert.date.strftime("%A, %B %d, %Y @ %I:%M") %></h4>
	<h4>Price: $<%= @concert.price %></h4>
	<h6>Description: <%= @concert.description %></h6>
	</div>

	<div class="col-sm-12">
	<h4>Comments:</h4>
			
		<%@comments.each do |comment_concert| %>
			<span class="col-sm-12 comment">
			  <li>
			  	<%=comment_concert.comment%>
			  </li>
			</span>
		<% end %>
		<br>
		
	</div>
	
	<div class="col-sm-12">
	<%= form_for [@concert, @comment] do |f| %>
		<%= f.label :comment %>
		<%= f.text_field :comment %>
		<%= f.submit 'Save' %>
	<% end %>
	</div>
  </div>
</div>
</body>
</html>