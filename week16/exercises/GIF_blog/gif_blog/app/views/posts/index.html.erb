<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<title>GIFblog</title>
</head>
<body>
	<h1>This is the list of posts</h1>

	<ul>
		<% @posts.each do |post| %>
			<li> 
				<strong><%= post.id %> <%= post.title %></strong>
				<br>
				<p><%= "this gif has this many votes: #{post.nov}" %></p>
				<%= link_to(post.title, posts_path(@post)) %>
			</li>
		<br>
	<% end %>
	</ul>

	<p><%= link_to("Create New", posts_new_path) %></p>
</body>
</html>